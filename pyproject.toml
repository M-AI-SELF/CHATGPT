[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "m-ai-self-omega-runtime"
version = "0.1.0a0"
description = "Minimal executable nucleus for the M-AI-SELF Cognitive Operating Environment"
requires-python = ">=3.11"
dependencies = []

[tool.pytest.ini_options]
pythonpath = ["."]
